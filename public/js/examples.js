<script>
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
String.prototype.firstUp = function(lower) {
    return (lower ? this.toLowerCase() : this).replace(/(?:^|\s)\S/g, function(a) { return a.toUpperCase(); });
    };  
let tekst = "pochmurnie";
let rezultat = tekst.firstUp();
console.log(rezultat); // Pochmurnie
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
function token(minut){
		minut = minut || 1;
		return Math.round(((new Date()).getTime())/(minut * 60000));
	}
let test = token(20);
console.log(test); // 1370691
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
Number.prototype.okolo = function(ok) {return Math.round(this.valueOf() * 10)/10;};  
console.log((2.87899).okolo()); // 2.9
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
String.prototype.pad = function(i)  {return ("0000" + this).slice(-i);}
console.log(("251").pad());  // 0000251
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
function testScreen(){
        let x=window.innerWidth,
        y = window.innerHeight;
        let t=null;
        if (screen.orientation.type) t = screen.orientation.type | null;
        let a=0;
        if (screen.orientation.angle) a = screen.orientation.angle;
        return {x:x,y:y,a:a,t:t};
    }
console.log(testScreen());
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

  
</script>
